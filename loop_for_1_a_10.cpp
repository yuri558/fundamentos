 #include <stdio.h> // Biblhioteca padrão 

int main() { //Estrutura para começar todo o programa.

    int i; //Variavel do tipo inteiro.

    for(i = 1; i <= 10; i++) { // A estrutura for diz que enquanto I for menor que dez dicionara mais um ate ele ficar igual a dez 
        printf("%d\n", i); // Aqui sera impresso a mensagem relacionada ao comando for
    }

    return 0; // Return zero quer dizer que o progrma executou sem erros.
}
